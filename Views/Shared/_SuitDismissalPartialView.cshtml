@model SAMLitigation.Models.ViewModel.SAM_Litigation_DetailsViewModel

<div class="suit-dismissal-card mb-3">
    <div class="card shadow-sm">
        <div class="card-header bg-danger text-white d-flex justify-content-between align-items-center">
            <div>
                <span class="badge badge-light mr-2" style="font-size:1em">Suit Dismissal</span>
                <strong style="font-size:2em">@Model.CaseNumber</strong>
            </div>
            <div class="dismissal-date">
                <i class="fas fa-calendar-alt"></i>
                @Model.ActionTakenDate.ToString("dd-MMM-yyyy")
            </div>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <div class="info-item">
                        <label class="info-label">
                            <i class="fas fa-landmark text-danger"></i> Court Name
                        </label>
                        <div class="info-value">@Model.CourtName</div>
                    </div>
                </div>
                <div class="col-md-6 mb-3">
                    <div class="info-item">
                        <label class="info-label">
                            <i class="fas fa-user-tie text-danger"></i> Lawyer Name
                        </label>
                        <div class="info-value">@Model.LawyerName</div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <div class="info-item">
                        <label class="info-label">
                            <i class="fas fa-comment-dots"></i> Prior Comments
                        </label>
                        <div class="info-value text-muted">
                            @(string.IsNullOrEmpty(Model.CommentsPriorAction) ? "N/A" : Model.CommentsPriorAction)
                        </div>
                    </div>
                </div>
                <div class="col-md-6 mb-3">
                    <div class="info-item">
                        <label class="info-label">
                            <i class="fas fa-comment"></i> Post Comments
                        </label>
                        <div class="info-value text-muted">
                            @(string.IsNullOrEmpty(Model.CommentsPostAction) ? "N/A" : Model.CommentsPostAction)
                        </div>
                    </div>
                </div>
            </div>

            <div class="row mt-2">
                <div class="col-12">
                    <div class="action-buttons-div d-flex gap-2">
                        <button type="button"
                                class="btn btn-warning flex-fill"
                                onclick="handleEditDetail(@Model.LitigationDetailID)"
                                title="Edit this dismissal detail">
                            <i class="fas fa-edit"></i> Edit
                        </button>
                        <button type="button"
                                class="btn btn-info flex-fill"
                                onclick="viewDocument(@Model.LitigationDetailID)"
                                title="View/Add documents for this dismissal">
                            <i class="fas fa-file-alt"></i> Documents
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .suit-dismissal-card {
        transition: all 0.3s ease;
    }

        .suit-dismissal-card:hover {
            transform: translateY(-3px);
        }

        .suit-dismissal-card .card {
            border-left: 5px solid #dc3545;
        }

        .suit-dismissal-card .card-header {
            background-color: #dc3545 !important;
            font-weight: 600;
            padding: 0.75rem 1rem;
        }

    .dismissal-date {
        font-weight: 500;
    }

    .info-item {
        margin-bottom: 0.5rem;
    }

    .info-label {
        font-weight: 600;
        font-size: 0.875rem;
        color: #495057;
        margin-bottom: 0.25rem;
        display: block;
    }

        .info-label i {
            margin-right: 5px;
        }

    .info-value {
        font-size: 0.95rem;
        color: #212529;
        padding: 0.5rem;
        background-color: #f8f9fa;
        border-radius: 4px;
        min-height: 38px;
    }

    .action-buttons-div {
        padding-top: 0.5rem;
        border-top: 1px solid #dee2e6;
    }

        .action-buttons-div .btn {
            font-weight: 500;
            padding: 0.5rem 1rem;
            transition: all 0.2s ease;
        }

            .action-buttons-div .btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
            }

        .action-buttons-div .btn-warning {
            background-color: #ffc107;
            border-color: #ffc107;
            color: #333;
        }

            .action-buttons-div .btn-warning:hover {
                background-color: #ffb300;
                border-color: #ffb300;
                color: #000;
            }

        .action-buttons-div .btn-info {
            background-color: #a3b7ca;
            border-color: #17a2b8;
            color: #fff;
        }

            .action-buttons-div .btn-info:hover {
                background-color: #a3b7ca;
                border-color: #138496;
            }

    /* Gap utility for older Bootstrap versions */
    .gap-2 {
        gap: 0.5rem;
    }

    /* Flex-fill for older Bootstrap versions */
    .flex-fill {
        flex: 1 1 auto;
    }
</style>